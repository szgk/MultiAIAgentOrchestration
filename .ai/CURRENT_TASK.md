# Task

## Goal

Pythonでシンプルなカウンターモジュールを実装する。

## Requirements

- `Counter` クラスを実装する
- `increment()` でカウントを1増やす
- `decrement()` でカウントを1減らす
- `reset()` でカウントを0に戻す
- `value` プロパティで現在の値を取得できる
- カウントが0未満にならないようにする

## Constraints

- 外部ライブラリは使用しない
- 単一ファイル（`counter.py`）で実装する
